# index.html однажды залили через веб-интерфейс, и загрузка переписала все
# переводы строк на CRLF: следующий diff показал изменённым весь файл —
# 63 852 строки вместо реальных двадцати. Ревью в таком виде невозможно.
index.html text eol=lf

# Фикстуру не трогать: CRLF в CSV законен (RFC 4180), и проверки читают её
# такой, какая она есть.
tests/fixtures/meta_ru.csv -text
